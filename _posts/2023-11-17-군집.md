---
layout: single
title: 군집
excerpt: 
writer: Kiwiio
categories: 
    - ML+DL
tag: [clustering, ML]
toc: true
toc_sticky: true
author_profile: false
date: 2023-11-13
sidebar: 
    nav: "docs"

---

# 군집

머신러닝 비지도 학습에 속하며, 비슷한 데이터끼리 ‘군집’을 만든다. 

대표적으로 k-means가 존재한다. 

### **K-means Clustering**

K-means clustering이란 현재 가장 대중적으로 사용되는 clustering 방법 중 하나로, 특정한 centroid 개수를 설정하면 해당 수로 군집을 만들어준다. 여기서 K는 각 군집의 중심(centroid)의 개수를 나타내며, means는 말 그대로 평균을 얘기해서 '평균 군집화'라고 한다.

step 1. 각 데이터 포인트 i에 대해 가장 가까운 중심점을 찾고, 그 중심점에 해당하는 군집 할당

step 2. 할당된 군집을 기반으로 새로운 중심 계산, 중심점은 군집 내부 점들의 좌표의 평균(mean)으로 함

step 3. 각 클러스터의 할당이 바뀌지 않을 때까지 반복

![clustering](https://github.com/kkiwiio/kkiwiio.github.io/assets/121476928/9ade0a17-995b-407e-9970-fb46d94a0726)

### 최적의 k값 찾기

군집의 개수 K는 사용자가 임의로 정하는 것이기 때문에, 데이터에 최적화된 K를 찾기 어려움

- K를 설정하는 대표적인 방법은 Elbow method,Silhouette method 등이 있음

1.**엘보우 방법**

엘보우 방법은 K-means 알고리즘을 실행할 때, 클러스터 개수(K)를 점차 증가시키면서 클러스터링을 수행하고, 이에 따른 SSE(Sum of Squared Errors) 값을 계산하여 그래프로 나타내어 최적의 K값을 선택하는 방법

SSE란 각 데이터 포인트와 해당 클러스터 중심점 사이의 거리를 제곱한 값의 합을 의미. 

SSE 값이 작을수록 클러스터링의 성능이 좋다고 판단할 수 있다. 

그래서, 엘보우 방법에서는 클러스터 개수(K)를 증가시키면서 SSE 값을 계산하고, 이를 그래프로 나타낸다
이때, SSE 값이 감소하는 정도가 급격하게 줄어드는 지점을 최적의 K값으로 선택한다. 

![elbow](https://github.com/kkiwiio/kkiwiio.github.io/assets/121476928/c91e02d2-feb5-4a47-84c6-3f9218939faa)


2.**실루엣 분석**

각 데이터 포인트가 속한 클러스터의 일관성을 측정하여 최적의 K값을 찾는 방법

각 데이터 포인트의 실루엣 계수를 계산하여, 전체 데이터 포인트의 평균 실루엣 계수를 구합니다. 실루엣 계수는 클러스터의 일관성을 나타내며, 값이 높을수록 클러스터링 결과가 좋다는 것을 의미

실루엣 계수 계산방법

1. 데이터 포인트 i와 같은 클러스터 내의 모든 데이터 포인트 간의 거리의 평균을 a(i)로 계산합니다.
2. 데이터 포인트 i와 가장 가까운 클러스터의 모든 데이터 포인트 간의 거리의 평균을 b(i)로 계산합니다.
3. 데이터 포인트 i의 실루엣 계수 s(i)는 (b(i) - a(i)) / max(a(i), b(i))로 계산됩니다.

![silhouette](https://github.com/kkiwiio/kkiwiio.github.io/assets/121476928/a5ef5247-789c-43d9-9dc7-66f5286ac0ef)



가장 높은 실루엣 점수를 가지는 K값인 K=2가 최적의 K값이라고 할 수 있다. 

실루엣 분석은 엘보우 방법과 달리 SSE 값이나 클러스터 개수를 고려하지 않고, 오직 클러스터링의 성능만을 고려한다. 따라서, 엘보우 방법과 실루엣 분석을 함께 사용하여 최적의 K값을 탐색하는 것이 좋다.